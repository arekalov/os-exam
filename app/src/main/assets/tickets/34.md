---
number: 34
title: Язык XPath. Функции с множествами. Строковые, логические и числовые функции.
---

**Функции с множествами (node-set)** — операции над наборами узлов.

**Объединение `|`:** `//div[@class='header'] | //div[@class='footer']` — все div header или footer.

**Пересечение** (XPath 1.0 — через предикат): `//div[@class='item'][@data-active='true']`

**Разность** — через `not()`: `//input[not(@type='hidden')]`

**Функции:**
* `count(//tr)` — число узлов
* `sum(//item/@qty)` — сумма значений

**Предикаты как фильтрация:**
* `//tr[td[3]='Active']` — tr, где 3-й td = Active
* `//div[.//span[@class='error']]` — div с ошибкой внутри
* `//li[count(./ul/li) > 0]` — li с вложенным списком

**Оси** формируют множества: `//form/descendant::input`, `//td/preceding-sibling::td`

В Selenium (XPath 1.0): `|`, предикаты, оси, `count()`, `not()`, `contains()`.

**Строковые:**
* `string()`, `concat(s1,s2)`, `contains(hay,needle)`, `starts-with(s,prefix)`
* `substring(s,start,len)`, `substring-before/after(s,delim)`
* `string-length()`, `normalize-space()`, `translate(s,from,to)`

**Логические:**
* `boolean()`, `not()`, `true()`, `false()`, `lang()`
* Операторы в предикатах: `and`, `or`, `=`, `!=`, `<`, `>`, `<=`, `>=`

**Числовые:**
* `number()`, `sum()`, `floor()`, `ceiling()`, `round()`
* Арифметика: `//product[number(@price) < 100]`, `position() mod 2 = 1`

**Примеры:**
* `//a[starts-with(@href,'/products/')]`
* `//input[contains(@placeholder,'email') and not(@disabled)]`
* `//button[normalize-space(text())='Save']`
* `//tr[number(td[4]) > 1000]`

**XPath 1.0 (Selenium):** сравнение строк лексикографическое; для чисел — `number()`.
